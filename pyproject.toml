[project]
name = "ticket-pilot"
version = "2.1.1"
description = "Ticket Pilot for Autotask: secure mobile work logging, review, and submission."
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13.2",
    "fastapi>=0.115.0",
    "faster-whisper>=1.1.0",
    "httpx>=0.27.0",
    "itsdangerous>=2.2.0",
    "jinja2>=3.1.4",
    "psycopg[binary]>=3.2.1",
    "python-multipart>=0.0.9",
    "sqlalchemy>=2.0.32",
    "uvicorn[standard]>=0.30.6",
    "webauthn>=2.8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.2",
    "pytest-cov>=5.0.0",
    "ruff>=0.6.2",
]

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ticket_pilot"]

[tool.hatch.build.targets.wheel.force-include]
"AI_HELPER.md" = "ticket_pilot/AI_HELPER.md"
"USER_MANUAL.md" = "ticket_pilot/USER_MANUAL.md"
"WEB_CHANGELOG.md" = "ticket_pilot/WEB_CHANGELOG.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 160
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["B008"]
